# Operational references

- `threat-model.md`: protected assets, actors, assumptions, trust boundaries, threats, and controls.
- `adaptation-profiles.md`: central, domain, adopter, and release-contract configuration layers.
- `production-roadmap.md`: deployment work packages and accreditation gates beyond the offline core.

These documents describe implementation and deployment. Versioned request and evidence semantics are enforced by the code and JSON Schemas.
